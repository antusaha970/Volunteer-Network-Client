import React from 'react';
import { useLocation } from 'react-router-dom';

const AddEvent = () => {
    return (
        <div>
            This is add event
        </div>
    );
};

export default AddEvent;